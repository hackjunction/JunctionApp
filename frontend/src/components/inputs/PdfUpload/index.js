import React from 'react';

import withSnackbar from 'notistack';

const PdfUpload = ({ value, onChange, uploadUrl, resizeMode = 'contain', enqueueSnackbar, idToken }) => {
    return null;
};

export default withSnackbar(PdfUpload);
