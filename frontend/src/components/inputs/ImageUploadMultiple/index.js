import React from 'react'

const ImageUploadMultiple = (
    value = [],
    onChange,
    uploadUrl,
    resizeMode = 'contain',
    enqueueSnackbar,
    idToken,
) => {
    return <h1>Hello</h1>
}

export default ImageUploadMultiple
